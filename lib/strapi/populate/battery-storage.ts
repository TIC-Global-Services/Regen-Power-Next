export const hero =
  "populate[sections][on][battery-storage.hero][populate]=backgroundImage";

export const debsRebate =
  "populate[sections][on][battery-storage.debs-rebate][populate]=image";

export const jargon =
  "populate[sections][on][battery-storage.jargon][populate][cards][populate]=image";

export const billImpact =
  "populate[sections][on][battery-storage.bill-impact][populate][cards][populate]=image";

export const rangeGrid =
  "populate[sections][on][battery-storage.range-grid][populate][batteries][populate]=image";

export const installationTimeline =
  "populate[sections][on][battery-storage.installation-timeline][populate][steps][populate]=image";

export const team =
  "populate[sections][on][battery-storage.team][populate][cards][populate]=image";

export const customerStories =
  "populate[sections][on][battery-storage.customer-stories][populate][stories][populate]=image";
