export const hero =
  "populate[sections][on][battery-storage.hero][populate]=backgroundImage";

export const marquee =
  "populate[sections][on][battery-storage.marquee][populate][items]=true";

export const debsRebate =
  "populate[sections][on][battery-storage.debs-rebate][populate]=image";

export const jargon =
  "populate[sections][on][battery-storage.jargon][populate][cards][populate][image]=true";

export const billImpact =
  "populate[sections][on][battery-storage.bill-impact][populate][cards][populate][image]=true";

export const rangeGrid =
  "populate[sections][on][battery-storage.range-grid][populate][batteries][populate][image]=true";

export const capacityBlocks =
  "populate[sections][on][battery-storage.capacity-blocks][populate][cards]=true";

export const greatFit =
  "populate[sections][on][battery-storage.great-fit][populate][goodFitItems]=true" +
  "&populate[sections][on][battery-storage.great-fit][populate][conversationItems]=true";

export const solarMeaning =
  "populate[sections][on][battery-storage.solar-meaning][populate][cards]=true";

export const installationTimeline =
  "populate[sections][on][battery-storage.installation-timeline][populate][steps][populate][image]=true";

export const team =
  "populate[sections][on][battery-storage.team][populate][cards][populate][image]=true";

export const customerStories =
  "populate[sections][on][battery-storage.customer-stories][populate][stories][populate][image]=true";

export const faq =
  "populate[sections][on][shared.faq][populate][image]=true" +
  "&populate[sections][on][shared.faq][populate][items]=true";

export const ctaBanner =
  "populate[sections][on][shared.cta-banner][populate]=backgroundImage";
